services:
  be-client:
    build:
      context: .
      dockerfile: Dockerfile
    env_file:
      - .env
    environment:
      PORT: ${PORT:-3001}
    ports:
      - "${PORT:-3001}:3001"
    restart: unless-stopped
